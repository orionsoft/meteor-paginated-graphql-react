import reactCSS from 'reactcss'

export default reactCSS({
  'default': {
    th: {
      position: 'relative'
    },
    thSort: {
      cursor: 'pointer'
    },
    selected: {
      backgroundColor: '#cacaca',
      cursor: 'pointer'
    }
  }
})
